<table>
	<% @posts.each do |po| %>
		<tr>
			<td><%= po.title %></td>
			<td><%= po.price %></td>
			<td><%= link_to 'show', post_path(po) %></td>
		</tr>
	<% end %> 
</table>

<%= link_to "logout", posts_logout_path %>
<%= link_to "New", new_post_path %>